document.getElementById("bock_click1").addEventListener("click", function() {
    var contenidoManipulacion = document.getElementById("contenido-1");

    // Verificar el estado actual del contenido de manipulación
    if (contenidoManipulacion.style.display === "block") {
        // Si está visible, ocultarlo
        contenidoManipulacion.style.display = "none";
    } else {
        // Si está oculto, mostrarlo
        contenidoManipulacion.style.display = "block";
    }

});
document.getElementById("bock_click2").addEventListener("click", function() {
    var contenidoEmocional = document.getElementById("contenido-2");

    // Verificar el estado actual del contenido emocional
    if (contenidoEmocional.style.display === "block") {
        // Si está visible, ocultarlo
        contenidoEmocional.style.display = "none";
    } else {
        // Si está oculto, mostrarlo
        contenidoEmocional.style.display = "block";
    }
});
document.getElementById("bock_click3").addEventListener("click", function() {
    var contenidoEmocional = document.getElementById("contenido-3");

    // Verificar el estado actual del contenido emocional
    if (contenidoEmocional.style.display === "block") {
        // Si está visible, ocultarlo
        contenidoEmocional.style.display = "none";
    } else {
        // Si está oculto, mostrarlo
        contenidoEmocional.style.display = "block";
    }
});
document.getElementById("bock_click4").addEventListener("click", function() {
    var contenidoEmocional = document.getElementById("contenido-4");

    // Verificar el estado actual del contenido emocional
    if (contenidoEmocional.style.display === "block") {
        // Si está visible, ocultarlo
        contenidoEmocional.style.display = "none";
    } else {
        // Si está oculto, mostrarlo
        contenidoEmocional.style.display = "block";
    }
});
document.getElementById("bock_click5").addEventListener("click", function() {
    var contenidoEmocional = document.getElementById("contenido-5");

    // Verificar el estado actual del contenido emocional
    if (contenidoEmocional.style.display === "block") {
        // Si está visible, ocultarlo
        contenidoEmocional.style.display = "none";
    } else {
        // Si está oculto, mostrarlo
        contenidoEmocional.style.display = "block";
    }
});
document.getElementById("bock_click6").addEventListener("click", function() {
    var contenidoEmocional = document.getElementById("contenido-6");

    // Verificar el estado actual del contenido emocional
    if (contenidoEmocional.style.display === "block") {
        // Si está visible, ocultarlo
        contenidoEmocional.style.display = "none";
    } else {
        // Si está oculto, mostrarlo
        contenidoEmocional.style.display = "block";
    }
});
document.getElementById("bock_click7").addEventListener("click", function() {
    var contenidoEmocional = document.getElementById("contenido-7");

    // Verificar el estado actual del contenido emocional
    if (contenidoEmocional.style.display === "block") {
        // Si está visible, ocultarlo
        contenidoEmocional.style.display = "none";
    } else {
        // Si está oculto, mostrarlo
        contenidoEmocional.style.display = "block";
    }
});
document.getElementById("bock_click7").addEventListener("click", function() {
    var contenidoEmocional = document.getElementById("contenido-8");

    // Verificar el estado actual del contenido emocional
    if (contenidoEmocional.style.display === "block") {
        // Si está visible, ocultarlo
        contenidoEmocional.style.display = "none";
    } else {
        // Si está oculto, mostrarlo
        contenidoEmocional.style.display = "block";
    }
});
document.getElementById("bock_click7").addEventListener("click", function() {
    var contenidoEmocional = document.getElementById("contenido-9");

    // Verificar el estado actual del contenido emocional
    if (contenidoEmocional.style.display === "block") {
        // Si está visible, ocultarlo
        contenidoEmocional.style.display = "none";
    } else {
        // Si está oculto, mostrarlo
        contenidoEmocional.style.display = "block";
    }
});